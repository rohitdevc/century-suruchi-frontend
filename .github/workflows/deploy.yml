name: Deploy Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code
        run: |
          cd /home/centuryrealestate/century-suruchi/frontend
          GIT_SSH_COMMAND="ssh -i /home/readOnlyRepo -o IdentitiesOnly=yes" git pull origin main